on: [push]
name: Test
jobs:
  readmeExample:
    name: "[TEST] README example"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: "12.x"
      - run: "npm ci"
      - run: "npm run build"
      - name: "Get latest release of ${{ github.repository }}"
        uses: ./
        id: stats
        with:
          id: ${{ secrets.APP_ID }}
          privateKey: ${{ secrets.PRIVATE_KEY }}
      - run: "installations: '${{ steps.stats.outputs.installations }}'"
      - run: "most popular repositories: '${{ steps.stats.outputs.popular_repositories }}'"
